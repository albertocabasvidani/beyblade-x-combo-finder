---
name: web-frontend
status: active
updated: 21/09/2026
health: yellow
next-step: "Fase 7: scalare i contenuti (~27 pagine parte, ~100 combo) mentre si attende l'esito delle revisioni US/ES/FR"
blocked-by: null
current-plan: "C:\\Users\\cinqu\\.claude\\plans\\mi-hanno-espulso-dall-affiliazione-zany-squid.md"
main-doc: CLAUDE.md
---

# Sito web (Astro + Preact)

## Scope

Il sito statico (Astro SSG + island Preact, Tailwind v4, **monolingua inglese** servito dalla root)
che consuma `data/combos.json` e `data/parts.json` per la ricerca combo client-side. Include i
selettori parte per linea (BX/UX e CX), i badge di autorevolezza CAS e i filtri torneo. Deploy su
GitHub Pages via Actions. L'infrastruttura i18n IT resta in repo (dormiente), riattivabile in futuro.

## Backlog

<!-- Idee, feature, task non avviati. Formato: `- gg/mm/aaaa — testo` -->
- 16/06/2026 — pagine dettaglio combo `/combo/[id]` (SSG) per SEO (non ancora implementate)
- 16/06/2026 — SEO: structured data JSON-LD (la sitemap c'è dall'11/09/2026)
- ~~16/06/2026 — registrare Amazon Associates US~~ **chiuso il 19/09/2026**: esisteva già un account US (`albertocabasv-20`, tag storico diverso dai `-21`/`-22`), lista siti vuota. Aggiunto `com` in config, dichiarato beybladexcombos.com nella sitelist, `defaultMarketplace` tornato `com`. Resta l'informativa fiscale USA (Incomplete, la può compilare solo il titolare): blocca solo il pagamento, non il tracciamento
- 11/09/2026 — Vale la pena rendere `INITIAL_COMBOS`/`PAGE` scroll-infinito invece del bottone «Show more»? Guardare l'evento `load_more` in PostHog prima di toccare

## Known issues

<!-- Bug noti, problemi aperti, debiti tecnici. Formato: `- gg/mm/aaaa — testo` -->
- 19/09/2026 — **La revisione degli account US (`albertocabasv-20`) e ES (`beybladexcomb-21`, ri-candidato lo stesso giorno) arriva probabilmente entro qualche settimana**, secondo l'utente, che ha scelto di sottomettere subito invece di aspettare il rodaggio del contenuto. Il sito ha 18 pagine indicizzabili e 11 pubblicazioni scritte (chiuse le fasi 0-6 del piano), quindi il profilo di contenuto è già diverso da quello che ha fatto cadere l'account ES precedente — ma è un rischio corso deliberatamente su un sito appena pubblicato, non una certezza
- 19/09/2026 — **Il sito non espone un solo link Amazon a chi non esegue JavaScript**: i link li costruisce l'isola Preact al click (verificato il 19/09 su `dist/index.html`, zero occorrenze di `www.amazon.`). Per un browser è indifferente, per un crawler no — e non si sa quale dei due sia il revisore Amazon. Le pagine editoriali del piano contenuti nascono con i link pre-renderizzati lato server; la home resta così
- 12/09/2026 — Finché AdSense non approva il sito, i quattro slot lasciano spazio vuoto: 100 px per i banner in cima e in fondo (320x100 su mobile, 728x90 da 800 px in su) e 250 px per i due rettangoli, misurati sul dominio. Il collasso su `data-ad-status="unfilled"` in `global.css` li toglie solo quando Google risponde, e senza consenso alla CMP Google non risponde affatto. Si chiude da sé con l'approvazione; se la revisione fosse respinta, la via è svuotare gli id in `AD_SLOTS`
- 12/09/2026 — Session replay di PostHog acceso l'11/09 ma **mai verificato**: sono stati controllati gli eventi in Activity, non che una registrazione compaia in Session replay. Da guardare al primo traffico vero (il Chrome dell'utente non serve: un'estensione gli blocca le POST verso `eu.i.posthog.com`)

## In progress

<!-- Lavori in corso. Se collegati a un piano in plans/, linkalo. -->
- 19/09/2026 — **Contenuti editoriali per reggere la revisione Associates** (piano: `C:\Users\cinqu\.claude\plans\mi-hanno-espulso-dall-affiliazione-zany-squid.md`). Amazon ha chiuso l'account ES il 19/09 contestando, sul sito combo, contenuto insufficiente e link senza tracking ID; il sito dichiarato è lo stesso su tutti i portali. Fatto: fasi 0-6 complete — content collections, rotte `/meta/`, `/parts/`, `/combos/`, `/buy/` con link Amazon pre-renderizzati lato server, JSON-LD, breadcrumb, disclosure nel contenuto, 11 pubblicazioni scritte (7 da agenti Sonnet in parallelo, 2 report mensili a mano), sitemap da 3 a 18 URL, `test:content` verde. Fatto anche: rimosso `trottolebeybladex.it` dalla sitelist di DE/FR/UK (dove conviveva con beybladexcombos.com — stesso sito citato nel rifiuto ES), ri-candidata la Spagna lo stesso giorno con account nuovo `beybladexcomb-21` (l'utente ha scelto di sottomettere subito, non aspettare oltre). Il passo "Finalizar" del modulo Amazon richiede un click umano vero: un click automatico via JS o via coordinate dà «No se puede cargar el paso de verificación» — è l'utente che ha confermato. Fase 5 chiusa il 19/09: sezione `[14] Editoriale` in `e2e-smoke.ts` (sitemap vs conteggio pagine in `dist/`, H1 unico, canonical, JSON-LD valido, disclosure nel contenuto, ogni link Amazon con tag/sponsored/nofollow, nessun fetch di `combos.json`) — 199 OK / 0 KO sia in locale sia sul dominio pubblicato. Resta solo la fase 7: le altre ~27 pagine parte e ~100 combo
- 12/09/2026 — **AdSense: sito in revisione, slot già in pagina**. Le quattro unità display responsive esistono (`bxcombos-top` 2893429591, `-rail` 8552950383, `-infeed` 7497432742, `-bottom` 1580347920) e i loro id sono in `src/lib/ads-config.ts`, quindi la pagina serve già i tag `<ins>`; il sito è in stato «Getting ready» e nessun annuncio viene riempito finché non passa. `ads.txt` è passato a **Authorized** il 13/09/2026 (il 12 era ancora «Not found»: era solo il crawl di Google non ancora avvenuto). Resta: aspettare l'esito della revisione — il 13/09 lo stato era ancora «Getting ready» — e verificare che il messaggio di consenso compaia davvero a un visitatore in EEA
- 21/09/2026 — **Ricorso ad Amazon.fr pronto nel modulo, manca l'invio**. Il rifiuto francese contestava i link senza identificativo di tracciamento su beybladexcombos.com; il testo spiega il meccanismo di localizzazione (paese del visitatore → negozio e tag di quel paese, più il menù per scegliere a mano) e chiude dicendo che l'identificativo francese è sul sito. Compilato su `partenaires.amazon.fr/home/contact/` con oggetto «Demandes relatives à un compte rejeté», ID `bxcombosfr-21`, URL del sito. **Il modulo va compilato a mano: non basta far premere il pulsante a una persona.** Provato in tutti i modi — campi riempiti via JavaScript, digitati carattere per carattere con eventi CDP, scritti col tool `form_input` — e l'invio risponde sempre `{"is_valid":false,"errors":{"token":"Anti-automation validation failed"}}`, **anche quando a cliccare è l'utente pochi minuti dopo**. È la compilazione a invalidare il token, non il click né il tempo trascorso (ipotesi avanzata dopo il primo fallimento e smentita dal secondo). Un tentativo di invio fallito lascia poi la pagina in uno stato in cui compare «Impossible de charger l'étape de vérification» e il campo Objet si svuota. La lezione del «Finalizar» spagnolo del 19/09 era quindi più larga di come era stata scritta: su questi moduli Amazon l'automazione non può toccare nulla. Testo francese negli appunti e in `bbxdealmonitor/tmp/ricorso-francia-fr.txt`, istruzioni in `ricorso-francia.txt`. Resta: compilare a mano e inviare, poi attendere la risposta
- 12/09/2026 — **Associates IT in approvazione temporanea** (account nuovo `albertocv0b-21`, solo beybladexcombos.com dichiarato): Amazon rivede il sito dopo 3 vendite idonee da amazon.it, entro 180 giorni. Resta: portare traffico italiano al sito (canale WhatsApp, social) finché le 3 vendite non arrivano; se scade, ricandidarsi

## Changelog

<!-- Cose completate, dalla più recente. Formato: `- gg/mm/aaaa — testo` -->
- 21/09/2026 — **Il negozio Amazon segue il paese del visitatore, non più la lingua del browser**. Prima il mercato si deduceva solo da `navigator.languages`: chi arrivava da una ricerca con Chrome in inglese finiva sul `defaultMarketplace` (`com`) qualunque fosse il suo paese — ed è la contestazione del rifiuto francese. Ora `src/lib/marketplace.ts` ha una precedenza esplicita: scelta salvata → paese (da `cloudflare.com/cdn-cgi/trace`, riga `loc=XX`, senza chiave, CORS aperto, timeout 1,5 s, tenuto in `sessionStorage`; su GitHub Pages non esiste un header geo lato server) → lingua → default. Il mercato rilevato non si salva mai: solo la scelta manuale, che così non viene mai sovrascritta. Nuovo `src/components/market-picker.astro` nell'header (quindi su ogni pagina) e nei blocchi «Where to buy», con la nota che dice da dove viene la scelta — è quella riga a dare a chi naviga in VPN il modo di capire perché vede il negozio sbagliato. Stato unico per pagina (`window.__bxcfMarket` + evento `bxcf:market`), così i tre select restano allineati. Quando è l'utente a scegliere, i link portano `creatorsDisableRedirect=true`, altrimenti OneLink lo sposterebbe lo stesso: parametro preso dall'URL che Amazon stessa produce nei suoi redirect e verificato dalla Germania (col parametro amazon.com resta amazon.com, senza va su amazon.de). Nell'header il menù mostra solo il suffisso (`.it`, `.co.uk`): col dominio intero occupava un terzo della barra su mobile e mandava la navigazione a tre righe, visto sullo screenshot dell'e2e. Verifiche: `test:amazon` 55/55 (tabella paese→mercato, precedenza, en-US con indirizzo francese → fr, parametro anti-redirect solo sulla scelta manuale), `test:e2e` 211 OK / 0 KO con la nuova sezione `[15]` che simula il paese intercettando la sonda (FR → amazon.fr col tag francese; correzione manuale che resiste a un reload con paese diverso; sonda irraggiungibile → link comunque taggati), `tsc --noEmit` pulito
- 21/09/2026 — **Store collegati in OneLink, e misura di cosa fa davvero**. Sull'account IT collegati i quattro store disponibili (DE/ES/UK/US: Francia e Giappone non sono fra i collegabili) e impostati i tracking ID predefiniti su quelli del sito. Misurato subito dopo con Chrome da un indirizzo tedesco: il link `amazon.com` col tag US viene spostato su amazon.de, quello `amazon.it` col tag IT no, sei prove in 25 minuti — mentre lo strumento «Controlla prodotti corrispondenti» di Amazon dichiarava il redirect attivo. Il redirect lo fa la pagina Amazon, non un 30x, quindi con `curl` non si vede: va provato con un browser vero. Conclusione: OneLink non copre la localizzazione del sito, tantomeno per Francia e Giappone. Script di controllo lasciato sul VPS (`node /root/onelink-test-de.js` via `ssh vintedwatch-vps`)
- 19/09/2026 — **Verifica editoriale con browser reale (fase 5 chiusa)**. Nuova sezione `[14] Editoriale` in `scripts/e2e-smoke.ts`: legge la sitemap (fonte di verità, non un elenco scritto a mano) e per ogni pagina meta/parts/combos/buy controlla peso sotto 150 KB, un solo H1, canonical con slash finale, JSON-LD valido con `@type` noto, disclosure dentro `main` dove ci sono link Amazon, ogni link Amazon con `tag=`/`sponsored`/`nofollow`/`target=_blank`, nessun fetch di `/combos.json` (le pagine sono statiche, non devono scaricare il dataset della home). Aggiunto anche il confronto numero-URL-sitemap contro pagine HTML reali in `dist/`. 199 OK, 0 KO sia in locale sia su beybladexcombos.com — la verifica di prima era solo statica sull'HTML prodotto, questa apre davvero le pagine in Chrome
- 19/09/2026 — **Spagna ri-candidata, `trottolebeybladex.it` rimosso da DE/FR/UK**. Verificata la sitelist di tutti gli account attivi (IT/DE/FR/UK/JP): IT e JP avevano solo beybladexcombos.com, ma DE/FR/UK avevano ANCHE `trottolebeybladex.it` — lo stesso sito citato nel rifiuto ES per price-caching/price-alert. Rimosso da tutti e tre (`Websites and Mobile Apps updated` confermato su ognuno). Ri-candidata la Spagna lo stesso giorno: nuovo account `beybladexcomb-21` (il precedente, `bxcombos-21`, resta chiuso e il suo tag va considerato morto), sito dichiarato, profilo compilato (tipo "Una página web de contenido", provenienza "Búsqueda online"). Il modulo si è rotto due volte durante la compilazione — un timing bug nella lettura async della sitelist (letta prima che i dati caricassero via JS, falso "vuota" su UK/DE/FR/JP quando in realtà avevano già i siti) e un menù nativo `<select>` che blocca lo screenshot CDP finché resta aperto — risolti leggendo/scrivendo i campi via JavaScript invece che a schermo. Il click finale su "Finalizar" rifiuta l'automazione (click via JS o via coordinate → «No se puede cargar el paso de verificación»): richiede il gesto umano reale, fatto dall'utente. Tag `es` aggiornato in `data/amazon-config.json`. Verifica: dashboard del nuovo account raggiunta, `StoreID: beybladexcomb-21`, `test:amazon` verde
- 19/09/2026 — **Nessun link Amazon può più uscire senza tracking ID**. Il mercato si deduceva dalle lingue del browser e chi non matcchava nessuna regola — `en-US` compreso, cioè il revisore Amazon — finiva su `amazon.com`, che non ha account Associates e quindi tag: erano link non tracciati, ed è una delle quattro contestazioni della chiusura ES del 19/09. Tolto `com` da `data/amazon-config.json` (era anche `defaultMarketplace`), che così non compare più nemmeno nel select; `defaultMarketplace` è ora `uk`, e `marketFromLanguages(langs, available, fallback)` prende il fallback dalla config invece di avere `'com'` cablato dentro. `withTag()` **solleva** su tag vuoto invece di restituire l'URL nudo: la difesa sta nel codice e non nella config, perché la config la riscrive chiunque e un `throw` rompe la build. Costo accettato: un visitatore americano va su amazon.co.uk finché non esiste l'account US (voce a backlog). Le due suite asserivano il difetto e sono state invertite — `test:amazon` pretendeva «amazon.com senza tag», l'e2e saltava il controllo del tag proprio quando il mercato era `com`; al loro posto, l'invariante che ogni link ha `tag=` su ogni mercato, più un controllo sulla config vera (ogni marketplace ha un tag, il default pure) che il golden test faceva su una config finta. Verifiche: `test:amazon` 33/33, `test:e2e` 49/49 con Chrome in inglese — cioè nelle condizioni del revisore — build pulita
- 12/09/2026 — **«Buy parts» su ogni card e quattro posizionamenti AdSense**. Prima i link affiliati esistevano solo sui chip delle parti mancanti, quindi servivano una selezione e Compare attivo: chi guardava il ranking non vedeva nulla di acquistabile. Ora ogni card ha un pulsante «Buy parts» che apre un pannello con un link per ogni parte della combo (`buy-part`, `source: 'buy-parts'` nell'evento `amazon_click`, apertura tracciata come `buy_parts_opened`); con Compare attivo le parti possedute restano col ✓ e senza link. Chiuso di default: apre 3 link su BX e fino a 6 su CX, e sempre aperto allungherebbe 60 card — l'evento dice se conviene invertire. AdSense: slot `rail` (fondo del pannello parti, su mobile sopra il ranking) e `infeed` (dopo la 6ª card, poi ogni 20) come componente Preact `ad-unit.tsx`, con push una volta sola per istanza e key per posizione, perché la lista si ri-renderizza a ogni filtro; `top` e `bottom` restano `.astro`. Create le quattro unità display responsive su AdSense (`bxcombos-top`, `-rail`, `-infeed`, `-bottom`) e cablati gli id. Tre difetti trovati e chiusi durante la verifica: `<AdSlot slot="top" />` non renderizzava niente perché in Astro `slot` è una prop riservata (prop rinominata `name`); gli `<ins>` dell'isola resi anche lato server causavano un `no_div` intermittente (1 run su 3), ora sono client-only; e uno slot `auto` non riempito riserva ~390 px, cioè una schermata bianca in cima su mobile. Su quest'ultimo il primo rimedio (formato `horizontal` + collasso degli slot `unfilled`) funzionava in locale ma **non sul dominio vero**, dove AdSense non risponde affatto finché il visitatore non sceglie nel messaggio di consenso: senza risposta non c'è `unfilled` da collassare, e il buco resta. Gli slot hanno quindi dimensioni fisse (`ad-banner` 320x100 → 468x60 → 728x90, `ad-box` 300x250), così lo spazio riservato è noto e piccolo anche quando l'annuncio non arriva mai; il collasso su `unfilled` resta come secondo strato. Con gli annunci attivi il toggle del tema falliva nel test perché il click partiva a pagina scrollata (header non sticky, toggle a y=-106): corretto il test, non il sito. Verifiche: `tsc --noEmit` pulito, e2e 48/48 per tre run di fila, home 332 KB, prima schermata mobile senza buchi. Sul dominio vero (non in locale: la CMP si attiva solo sul sito registrato in AdSense) compare il cookie `FCCDCF` di Google Funding Choices, che ricorda la scelta sul consenso: dichiarato nella privacy policy e ammesso dal test, che per il resto continua a pretendere zero cookie

- 12/09/2026 — **Ricandidatura Associates IT** via Claude in Chrome dopo il rifiuto del 25/08 (motivo: nessun link con tracking ID sulle proprietà dichiarate, i social di papinerd). Dichiarato il solo beybladexcombos.com, tipo «Sito di Contenuti»; il puzzle anti-bot lo ha risolto l'utente. Tag `albertocv0b-21` scritto in `data/amazon-config.json` (`it`)
- 12/09/2026 — **Tag Associates dedicati e AdSense avviato**. Creati via Claude in Chrome i tracking ID bxcombos-21 (es), bxcombosde-21, bxcombosfr-21, bxcombosuk-21, bxcombos-22 (jp) e scritti in `data/amazon-config.json` (verificati nell'HTML pubblicato); beybladexcombos.com aggiunto alla lista siti dei 5 account. L'account IT non esiste più: `it` resta senza tag. AdSense: sito aggiunto all'account pub-7303361297226779, script in head e `public/ads.txt` pubblicati, proprietà verificata, revisione richiesta, CMP di Google a 3 scelte attivata. Gotcha: gli ID `-21` sono uno spazio di nomi unico fra i marketplace EU
- 12/09/2026 — **Dominio beybladexcombos.com e PostHog attivi**. DNS su Tophost (4 A + AAAA su @, CNAME www → albertocabasvidani.github.io), custom domain e Enforce HTTPS su GitHub Pages via `gh api`, `public/CNAME` e default di `astro.config.mjs` sulla root; il vecchio URL github.io redirige (301). Chiave del progetto PostHog EU (id 272532) in `analytics-config.ts`, Session replay acceso e `cookieless_server_hash_mode` portato a 2 via API (a 0, il default, l'ingest rispondeva 200 ma scartava tutti gli eventi cookieless: nessun evento in Activity finché non è stato cambiato; ora `part_added`/`search_results`/`period_changed` compaiono). Verifiche sul sito vero: home 312 KB, `combos.json` 200, e2e 42/42 con `E2E_URL=https://beybladexcombos.com`; ingest PostHog 200 da un Chrome pulito (UA normale) e evento `curl_probe` visibile in Activity. Trappole misurate: posthog-js scarta gli eventi da browser headless/webdriver (un probe headless non manda nulla, serve UA normale + `navigator.webdriver=false`); nel Chrome dell'utente le POST a `eu.i.posthog.com` falliscono («Failed to fetch», estensione che blocca), quindi il suo traffico non compare in PostHog
- 11/09/2026 — **SEO, privacy, predisposizione dominio e AdSense**. `astro.config.mjs` legge `SITE_ORIGIN`/`SITE_BASE` (default GitHub Pages) e monta `@astrojs/sitemap` (solo pagine HTML); `src/pages/robots.txt.ts` genera robots con l'URL della sitemap coerente; layout con canonical (slash finale come la sitemap) e Open Graph/Twitter card con `public/og.png` (1200×630, generata con sharp da `tmp/gen-og.mjs`). Pagina `/privacy/` (chi siamo, PostHog cookieless, AdSense e consenso Google, affiliazione Amazon, preferenze locali, diritti), link nel footer. AdSense predisposto e spento: `src/lib/ads-config.ts` vuoto → nessuno script né slot; `ad-slot.astro` sopra e sotto l'isola. Cartelle vuote `src/pages/api/buy/` rimosse. Build di prova in modalità dominio (`SITE_BASE=/`): link radice, robots/sitemap/og su beybladexcombos.com, e2e 41/42 (l'unico KO era il test stesso: build senza chiave PostHog di prova). Il passaggio vero al dominio è documentato in CLAUDE.md e scatta quando l'utente ha registrato il dominio: fino ad allora il sito resta sul github.io
- 11/09/2026 — **Analytics PostHog cookieless (cloud EU)** — `src/lib/analytics.ts` (`cookieless_mode: 'always'`: niente cookie né storage, niente banner; no-op in dev e finché la chiave in `analytics-config.ts` è il placeholder). Eventi: `part_added`/`part_removed`, `search_results` (fotografia della ricerca con debounce 500 ms), `filter_toggled` (compare, tournament, meta, buildable, linea, stadio), `period_changed`, `load_more`, `marketplace_changed`, `amazon_click`, `theme_toggled` (header), `source_click` (about). Cablaggio verificato con una chiave di prova: la preview contatta `eu.i.posthog.com` e non scrive cookie né localStorage (e2e verde). La chiave vera la mette l'utente
- 11/09/2026 — **Amazon riattivato**: link «Buy» sui chip delle parti mancanti (Compare attivo), `rel=sponsored`, marketplace dalle lingue del browser con select «Shop on» persistito, `/dp/ASIN` dove `data/amazon-asins.json` conosce il set (52 codici dal monitor, `npm run sync:amazon-asins`), altrimenti ricerca per nome (blade & co.) o per codice set (ratchet/bit). Tag per marketplace in `data/amazon-config.json` (tracking ID dedicati al sito, oggi vuoti). `buildPartLookup` preferisce i codici Takara Tomy a quelli Hasbro (bug latente: `accel → G1536`, 0 risultati su Amazon EU). Disclosure nel footer, sezione affiliazione in about. `npm run test:amazon` (20 controlli)
- 11/09/2026 — **Home da 38,5 MB a 279 KB** (31 KB gzip). `index.astro` passava tutto `combos.json` (19 MB con l'evidenza) come prop dell'isola Preact, e Astro lo serializza in un attributo HTML dove ogni `"` diventa `&quot;`: il doppio. Ora `src/lib/slim-combos.ts` proietta le combo sui soli campi usati dalla UI, l'endpoint `src/pages/combos.json.ts` le serve come asset separato (2,46 MB, 174 KB gzip) e l'isola parte con 30 combo inline (solo finestra 12M) e fetcha il resto. **Filtro periodo** 1M/3M/6M/12M (pill sopra il ranking, default 12M): ordina su `windows[p].score`, nasconde le combo senza risultati nella finestra, badge e filtri Tournament-proven/Meta valutano la finestra. **Paginazione** «Show more» a 60 card (prima 4.000 combo × 2 `<article>` in DOM). `score-badge.tsx` prende score/tag/soglie invece della combo; `search-engine.ts` lavora su `SlimCombo` con `period` obbligatorio; via `topCombosForBlade` (inutilizzata) e la chiave i18n orfana `search.showAll`. **`npm run test:e2e`** (`scripts/e2e-smoke.ts`, playwright-core + Chrome): percorso utente completo su desktop e mobile 390 px, 34 controlli verdi e 4 SKIP sulle parti Amazon/privacy non ancora fatte; `data-testid` su card, badge, pill periodo, contatore, Show more, chip owned/missing
- 17/06/2026 — sito monolingua EN su root (rimosso redirect a `/en/`, eliminate route `/en/` e `/it/`, about su `/about/`, header senza selettore lingua); pagina about: fonti ridotte a nome+link (tolti weight e chip type), filtrate alle fonti del CAS
- 16/06/2026 — badge di autorevolezza (CAS) + filtri torneo nella ricerca
